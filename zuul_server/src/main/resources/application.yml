server:
  port: 8005

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8091/eureka/

spring:
  application:
    name: zuul-service

#zuul:
#  routes:
#    old-calssmate:
#      path: /old-calssmate/**
#      serviceId: old-calssmate
#  host:
#    connect-timeout-millis: 3000
#    socket-timeout-millis: 3000
#
#old-calssmate:
#  ribbon:
#    listOfServers: http://localhost:8081

hystrix:
  command:
    default:
      execution:
        isolation:
          thread:
            timeout-in-milliseconds: 3000
